<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ config('app.name') }}</title><link rel="stylesheet" href="{{ asset('css/store.css') }}"><script src="https://checkout.razorpay.com/v1/checkout.js"></script></head><body><header class="navbar"><div class="nav-inner"><a class="brand" href="{{ route('home') }}"><span>🎮</span>GameTopup</a><nav class="nav"><a href="{{ route('home') }}">Games</a>@auth<a href="{{ route('wallet.index') }}">Wallet</a><a href="{{ route('orders.index') }}">Orders</a>@if(auth()->user()->role==='admin')<a class="active" href="{{ route('admin.dashboard') }}">Admin</a>@endif<form method="post" action="{{ route('logout') }}">@csrf<button>Logout</button></form>@else<a href="{{ route('login') }}">Login</a><a class="active" href="{{ route('register') }}">Register</a>@endauth</nav></div></header><main class="container">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert alert-error">{{ session('error') }}</div>@endif @if($errors->any())<div class="alert alert-error">{{ $errors->first() }}</div>@endif @yield('content')</main><footer class="footer">© {{ date('Y') }} GameTopup</footer></body></html>
